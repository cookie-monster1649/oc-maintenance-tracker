services:
  app:
    image: ghcr.io/cookie-monster1649/oc-maintenance-tracker:latest
    # build: .   ← uncomment to build locally instead of pulling the image
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    volumes:
      - ./data:/app/data
